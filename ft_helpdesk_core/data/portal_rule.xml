<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Portal users may only read tickets where they are the customer. -->
    <record id="rule_ft_helpdesk_ticket_portal" model="ir.rule">
        <field name="name">Helpdesk Ticket: portal users see own tickets</field>
        <field name="model_id" ref="model_ft_helpdesk_ticket"/>
        <field name="domain_force">[('customer_id', '=', user.partner_id.id)]</field>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="False"/>
        <field name="perm_create" eval="False"/>
        <field name="perm_unlink" eval="False"/>
    </record>

    <!-- Portal users may only read milestones that belong to their tickets. -->
    <record id="rule_ft_helpdesk_milestone_portal" model="ir.rule">
        <field name="name">Helpdesk Milestone: portal users see milestones of own tickets</field>
        <field name="model_id" ref="model_ft_helpdesk_milestone"/>
        <field name="domain_force">[('ticket_id.customer_id', '=', user.partner_id.id)]</field>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="False"/>
        <field name="perm_create" eval="False"/>
        <field name="perm_unlink" eval="False"/>
    </record>

</odoo>
